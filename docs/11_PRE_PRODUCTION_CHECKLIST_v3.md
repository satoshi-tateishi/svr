# 本番前チェックリスト

## 1. 認証

- [ ] `portal_jwt` ありで自動ログインできる
- [ ] 無効 JWT でログインされない
- [ ] email 既存ユーザーへ `portal_uuid` が正しく紐づく

## 2. `productions`

- [ ] 公演作成ができる
- [ ] セットアップ画面から工程構成を保存できる
- [ ] 人員申請一括編集が保存できる
- [ ] 車両申請一括編集が保存できる
- [ ] 前日コピー API が壊れていない
- [ ] 権限なしユーザーが HTMX 編集を実行すると 403 HTML が返る

## 3. `performances`

- [ ] 公演作成から工程展開まで通る
- [ ] 10 工程テンプレートが正順で展開される
- [ ] 人員重複割当が拒否される
- [ ] 車輌重複割当が拒否される
- [ ] 外注車輌は重複除外になる
- [ ] `lock_phase_slot()` が `applied_*` を保存する
- [ ] `lock_vehicle_operation()` が原価未設定時の警告を返す
- [ ] PDF 2 種が出力できる

## 4. データ不変性

- [ ] Lock 済みデータを解除する API が存在しないことを確認
- [ ] 帳票が `applied_*` を参照している
- [ ] 単価履歴の重複登録が拒否される

## 5. インフラ

- [ ] `docker compose up -d` で `db`, `redis`, `web` が起動する
- [ ] `svr_db` のヘルスチェックが通る
- [ ] WeasyPrint 依存が入ったイメージで PDF 生成できる
- [ ] `shin-on-internal` ネットワークが本番環境でも解決できる

## 6. まだ前提にしないこと

以下は未実装なので、リリース判定条件に含めない。

- [ ] AuditLog
- [ ] Unlock
- [ ] SaaS 連携
- [ ] Celery 非同期処理
